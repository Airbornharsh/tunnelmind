{
  "name": "tunnelmind",
  "version": "1.0.0",
  "description": "Distributed GPU/model sharing platform using tunneling",
  "private": true,
  "scripts": {
    "dev:cloud": "cd cloud-server && pnpm run dev",
    "dev:local": "cd local-server && pnpm run dev",
    "dev:frontend": "cd frontend && pnpm run dev",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,css,md}\"",
    "build": "cd cloud-server && pnpm run build && cd ../local-server && pnpm run build && cd ../frontend && pnpm run build",
    "install:all": "cd cloud-server && pnpm install && cd ../local-server && pnpm install && cd ../frontend && pnpm install"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "prettier": "^3.6.2",
    "typescript": "^5.3.3"
  }
}
